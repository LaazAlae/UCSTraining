services:
  - type: web
    name: ucs-training-portal
    env: static
    staticPublishPath: .
    buildCommand: echo "Ready to deploy"
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
